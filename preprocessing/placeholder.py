from __future__ import annotations

from typing import Any


def resize_image(image: Any, size: tuple[int, int] = (640, 640)) -> Any:
    """Placeholder resize step returning the input image unchanged."""
    return image


def remove_noise(image: Any) -> Any:
    """Placeholder noise removal step returning the input image unchanged."""
    return image


def enhance_image(image: Any) -> Any:
    """Placeholder enhancement step returning the input image unchanged."""
    return image


def normalize_image(image: Any) -> Any:
    """Placeholder normalization step returning a simple dummy value."""
    return image
